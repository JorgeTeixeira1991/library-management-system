spring:
  config:
    activate:
      on-profile: mcp
  ai:
    mcp:
      server:
        enabled: true
        name: library-system
        version: 1.0.0
        protocol: STREAMABLE
        type: SYNC
        annotation-scanner:
          enabled: true
